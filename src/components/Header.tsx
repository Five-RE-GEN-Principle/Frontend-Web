import styled from "styled-components";

const Header = () => {
  return (
    <Container>
      <div>test</div>
    </Container>
  );
};

const Container = styled.div`
  display: flex;
  width: 100vw;
  height: 80px;
`;

export default Header;
